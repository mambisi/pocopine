[package]
name = "agenkitty-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Wasm-safe Agenkitty framework contracts: specs, events, policy, sandbox descriptions, and tool-use planning types."

[dependencies]
pocopine-agenkit-core = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
toml = "0.8"
