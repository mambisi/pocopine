[package]
name = "pine-ui"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Unstyled, accessible UI components for pocopine — headless primitives (Button, Dialog, Popover, …) ready to style."

[lib]
# Published as `pine-ui` (the bare `pine` name is taken on crates.io), but the
# crate is still imported as `pine` — e.g. `use pine::Button`.
name = "pine"
crate-type = ["cdylib", "rlib"]
doctest = false

[dependencies]
pocopine = { path = "../pocopine" }
pocopine-core = { workspace = true }
pocopine-storage = { workspace = true }
wasm-bindgen = { workspace = true }
js-sys = { workspace = true }
serde = { workspace = true }
time = { version = "0.3", default-features = false, features = ["wasm-bindgen", "parsing", "macros"] }

[dependencies.web-sys]
version = "0.3"
features = [
    "CustomEvent",
    "DataTransfer",
    "DragEvent",
    "Element",
    "Event",
    "EventInit",
    "EventTarget",
    "AbortController",
    "AbortSignal",
    "Blob",
    "File",
    "FileList",
    "FocusEvent",
    "FocusEventInit",
    "CssStyleDeclaration",
    "HtmlElement",
    "HtmlInputElement",
    "HtmlTextAreaElement",
    "InputEvent",
    "KeyboardEvent",
    "KeyboardEventInit",
    "MouseEvent",
    "MouseEventInit",
    "PointerEvent",
    "PointerEventInit",
    "UiEvent",
    "Window",
    "console",
]

[dev-dependencies]
pocopine-core = { workspace = true }
serde = { workspace = true }
serde_json = "1"
wasm-bindgen-test = "0.3"
wasm-bindgen-futures = { workspace = true }
