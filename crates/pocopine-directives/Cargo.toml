[package]
name = "pocopine-directives"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Single source of truth for the pocopine pp-* directive surface, shared by the #[component] macro and the language server."

[dependencies]
